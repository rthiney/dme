/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ZipPipe } from './zip.pipe';

describe('Pipe: Zipe', () => {
  it('create an instance', () => {
    let pipe = new ZipPipe();
    expect(pipe).toBeTruthy();
  });
});